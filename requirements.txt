flask>=3.0
flask-limiter>=3.5

# Remote MCP endpoint. mcp was previously undeclared and only worked because it
# happened to be installed locally.
#
# Pinned below 2.0 deliberately: mcp 2.0.0 relocated mcp.server.fastmcp, and an
# unbounded ">=1.27" pulled it in and broke the deploy with ModuleNotFoundError.
# Moving to 2.x means porting mcp_server.py, not just lifting this bound.
mcp>=1.27,<2
uvicorn>=0.30,<1
a2wsgi>=1.10,<2       # runs the Flask (WSGI) app inside the Starlette (ASGI) one

# Retained so the old `gunicorn app:app` start command still works as a
# rollback; unused by server.py.
gunicorn>=21.0
